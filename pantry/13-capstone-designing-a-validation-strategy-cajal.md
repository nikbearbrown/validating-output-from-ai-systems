# CAJAL Figure Intelligence — Chapter 13 — Capstone: Designing a Validation Strategy for a Real System

Source: chapters/13-capstone-designing-a-validation-strategy.md
Mode: /scan silent
Date: 2026-05-29

## Density Recommendation
4 figure candidates. Mixed density. As a capstone, the chapter's value is in composing prior material into reusable structures: a seven-step decision flow (process), the layered strategy with an open-bottomed residual box (structural schematic, the book's thesis-image), and the strictness-dial comparison (same output type at two risk levels). Per-concept triage lands on 4 strong figures, no forcing.

## Figure 1: The Seven-Step Validation-Strategy Decision Flow
Priority: Critical
Trigger: MC — the strategy is a fixed procedure of seven interdependent steps run top to bottom (identify output types → set risk → deterministic floor → output-type layers → evaluator-where-justified → human gate → residual statement)
Figure type: Process flowchart
Concept statement: A validation strategy is produced by a fixed seven-step procedure that composes the whole book — identify output type(s), set risk from impact times reversibility, lay the deterministic floor, add output-type layers, add an evaluator only where justified, set the human gate by risk, and write the mandatory residual-risk statement.
Reader prior knowledge: Reader has the §13.2 numbered flow in prose; has not seen it drawn as a single top-to-bottom flowchart with the one branch (irreversible forces high) and the mandatory terminal step marked.
Source anchor: Section: 13.2 The decision flow

### Block 1 — Illustrae Paste Block
Create a blank, unannotated flat vector process flowchart running vertically top to bottom with seven sequential nodes connected by single-headed downward arrows. Render the seven nodes as rounded rectangles in this color logic: the first node (identify output types) and second node (set risk) in Blue #0072B2 as the primary intake; the third node (deterministic floor) in Bluish Green #009E73 to signal the always-present trustworthy band; the fourth node (output-type layers) and fifth node (evaluator where justified) in Orange #E69F00 as secondary additive layers; the sixth node (human gate) in neutral light gray; and the seventh and final node (residual-risk / cannot-certify statement) in Black #000000 with a heavier 1pt outline to mark it mandatory. Off the second node (risk), draw one short single-headed branch arrow to a small decision diamond in Vermillion #D55E00 representing "irreversible action forces high," looping back onto the main flow. Keep to: seven stacked nodes, one decision diamond, the connecting downward arrows, one branch arrow. Use 1pt strokes, single-headed arrows only, white background, no text anywhere. Flat vector, Okabe-Ito palette only.

### Block 2 — Full SCOPE Prompt
[S - Specification] single-column 89mm textbook width, 300 DPI, vector, white background, unannotated.
[C - Content] Seven sequential strategy steps as a vertical flow, plus one decision diamond for the irreversible-forces-high override, ending in an emphasized mandatory residual-statement node. Preserve: strict top-to-bottom order, the single override branch, and the terminal step's special status.
[O - Organization] Vertical chain of seven nodes; one branch off the risk node to a decision diamond; final node visually weighted.
[P - Presentation] Flat vector, Okabe-Ito: intake nodes Blue #0072B2, deterministic-floor node Bluish Green #009E73, additive-layer nodes Orange #E69F00, human-gate node neutral light gray, terminal residual node Black #000000 (heavier outline), override diamond Vermillion #D55E00; 1pt strokes; single-headed arrows only; no text in image.
[E - Exclusions] No step numbers, no step names, no "high/medium/low" words, no chapter citations, no legend.

### Block 3 — Negative Prompt
step numbers, step names, risk-level words, chapter citations, legend, text labels, words, gibberish letters, titles, captions, decorative borders, realistic textures, plastic wrap effects, drop shadows, gradient backgrounds, photographic elements, non-standard arrows, dual-headed arrows, hand-drawn styles, sketch lines, human figures, visual clutter, overlapping unaligned paths, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Figure 2: The Layered Strategy and the Box That Never Closes
Priority: Critical
Trigger: VG — the chapter's thesis-image is a structural hierarchy of bands (deterministic floor, output-type layers, evaluator band, human gate) terminating in an open-bottomed residual box, a "how it is organized" claim not depictable from text
Figure type: Structural schematic
Concept statement: A validation strategy renders top-to-bottom as a stack of bands — a mandatory deterministic floor, output-type layers, an evaluator band where justified, and a human gate sized to risk — beneath which sits the residual-risk box drawn open-bottomed on purpose, because at the frontier there is no layer to draw beneath the human and the box never fully closes.
Reader prior knowledge: Reader has the §13.4 description and the book's "works where ground truth is available, fails where it isn't" thesis; has not seen the stack drawn with the residual box's missing floor made literal.
Source anchor: Section: 13.4 The box that never closes

### Block 1 — Illustrae Paste Block
Create a blank, unannotated flat vector structural schematic: a vertical stack of horizontal bands, each band a full-width rectangle, stacked with no gaps from top to bottom. From top: band one (deterministic floor) in Bluish Green #009E73, drawn as the widest, most solid band to read as the trustworthy base layer; band two (output-type layers) in Blue #0072B2; band three (evaluator band) in Orange #E69F00, drawn slightly narrower / inset to signal "only where justified"; band four (human gate) in neutral light gray. Below band four, draw the residual-risk box in Vermillion #D55E00 — but draw it with its left, right, and TOP edges as solid 1pt lines and its BOTTOM edge ABSENT (open), so the box is literally open-bottomed with no floor. Inside the open box, place three small disconnected markers (the uncaught classes) in Vermillion #D55E00. Optionally widen the open box slightly toward the bottom to suggest it grows. Keep to: four stacked bands plus one open-bottomed residual box with three internal markers. Use 1pt strokes, white background, no arrowheads, no text anywhere. Flat vector, Okabe-Ito palette only.

### Block 2 — Full SCOPE Prompt
[S - Specification] single-column 89mm textbook width, 300 DPI, vector, white background, unannotated.
[C - Content] A top-to-bottom band stack — deterministic floor, output-type layers, evaluator band (inset), human gate — over an open-bottomed residual box containing three small uncaught-class markers. Preserve: the deterministic floor as the solid trustworthy base, the evaluator band as conditional/inset, and the residual box's deliberately missing bottom edge.
[O - Organization] Four gapless horizontal bands stacked vertically; residual box beneath with no bottom line, optionally flaring wider downward.
[P - Presentation] Flat vector, Okabe-Ito: deterministic floor Bluish Green #009E73, output-type band Blue #0072B2, evaluator band Orange #E69F00 (inset), human gate neutral light gray, residual box and its internal markers Vermillion #D55E00; 1pt strokes; no text in image.
[E - Exclusions] No band names, no "residual risk"/"floor"/"human gate" words, no chapter numbers, no legend, no arrows.

### Block 3 — Negative Prompt
band names, residual/floor/gate words, chapter numbers, legend, arrows, text labels, words, gibberish letters, titles, captions, decorative borders, realistic textures, plastic wrap effects, drop shadows, gradient backgrounds, photographic elements, non-standard arrows, dual-headed arrows, hand-drawn styles, sketch lines, human figures, visual clutter, overlapping unaligned paths, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Figure 3: The Strictness Dial — Same Output Type, Two Risk Levels
Priority: Critical
Trigger: VG — the §13.3 table makes the core "risk is the strictness dial" claim structural: holding output type fixed and changing only stakes adds layers and rewrites the residual, a how-it-scales relationship not depictable from prose
Figure type: Comparison panels
Concept statement: Holding the output type fixed (factual claims) and changing only the risk level adds validation layers and rewrites the residual statement — the low-risk strategy carries a thin stack and an advisory residual, the high-risk strategy adds full entailment, a second grounding check, a mandatory human gate, and a defer-to-a-professional residual — showing risk as the dial that sets strictness.
Reader prior knowledge: Reader has the §13.3 two-column table; has not seen the two strategies drawn as side-by-side stacks where the high-risk side is visibly taller with the added layers highlighted.
Source anchor: Section: 13.3 The same output type at two risk levels — the dial, made visible

### Block 1 — Illustrae Paste Block
Create a blank, unannotated flat vector figure with two side-by-side vertical band stacks sharing a common baseline at the bottom, the left stack labeled-by-position as low risk and the right as high risk. Build both stacks from the same band types so the shared layers align horizontally across the two: a deterministic-floor band in Bluish Green #009E73 (present in both, same height), an output-type/factual band in Blue #0072B2, and an evaluator band in Orange #E69F00. On the LEFT (low-risk) stack, stop after these three with a short residual box in neutral light gray on top. On the RIGHT (high-risk) stack, ADD two extra bands above the shared three — a second grounding band in Blue #0072B2 and a mandatory human-gate band in Vermillion #D55E00 — making the right stack visibly taller, then cap it with a residual box in Vermillion #D55E00 (the harder, defer-to-professional residual). Render the two added high-risk bands with a slightly heavier 1pt outline to mark them as the strictness increment. Keep to: two stacks, shared three bands, two extra bands on the right, two residual caps. Use 1pt strokes, white background, no arrows, no text anywhere. Flat vector, Okabe-Ito palette only.

### Block 2 — Full SCOPE Prompt
[S - Specification] single-column 89mm textbook width, 300 DPI, vector, white background, unannotated.
[C - Content] Two band stacks on a common baseline; identical lower three bands; the right (high-risk) stack adds a second grounding band and a mandatory human-gate band and is taller; each stack capped by a residual box, neutral on the left and Vermillion on the right. Preserve: same base layers, the added strictness on the high-risk side, and the changed residual.
[O - Organization] Two vertical stacks side by side, shared bands aligned across; extra bands only on the right; residual cap atop each.
[P - Presentation] Flat vector, Okabe-Ito: deterministic floor Bluish Green #009E73, factual/grounding bands Blue #0072B2, evaluator band Orange #E69F00, mandatory human-gate band Vermillion #D55E00, low-risk residual cap neutral light gray, high-risk residual cap Vermillion #D55E00; added bands heavier 1pt outline; no text in image.
[E - Exclusions] No "low risk"/"high risk" words, no band names, no table text, no legend, no arrows.

### Block 3 — Negative Prompt
low/high risk words, band names, table text, legend, arrows, text labels, words, gibberish letters, titles, captions, decorative borders, realistic textures, plastic wrap effects, drop shadows, gradient backgrounds, photographic elements, non-standard arrows, dual-headed arrows, hand-drawn styles, sketch lines, human figures, visual clutter, overlapping unaligned paths, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Figure 4: Per-Layer Catch/Miss Accounting (RAG Worked Example)
Priority: Important
Trigger: VG — the §13.3 worked example makes a structural claim (each layer catches one class and misses another, and the misses accumulate downward until mechanical ground truth runs out), a relationship across the layer stack not depictable from running prose
Figure type: Comparison panels
Concept statement: In the worked RAG strategy, each layer catches a specific failure class and lets another pass through to the next layer — schema catches shape, citation-existence catches fabrication, entailment catches unsupported claims — until the surviving misses (correct-but-incomplete, subtle paraphrase, stale-but-real source) reach the bottom with no mechanical oracle left to catch them.
Reader prior knowledge: Reader has the §13.3 step-by-step catch/miss prose; has not seen catches and misses drawn as a per-layer two-sided accounting with misses flowing down to the uncaught residual.
Source anchor: Section: 13.3 Worked end-to-end: a customer-facing RAG support assistant

### Block 1 — Illustrae Paste Block
Create a blank, unannotated flat vector figure with four stacked horizontal layer rows, each row split into a left half and a right half by a thin vertical divider. The four rows top to bottom are: deterministic floor, factual/entailment layer, evaluator screen, human gate. In each row, the LEFT half holds a small "caught" marker in Bluish Green #009E73 and the RIGHT half holds a small "missed" marker in Vermillion #D55E00. Draw one continuous single-headed arrow running down the RIGHT (missed) side, passing from each row's missed marker into the next row below, in neutral light gray, so the misses visibly propagate downward. At the bottom, the downward miss-arrow terminates in a small open residual box in Vermillion #D55E00 holding three tiny uncaught markers (the surviving classes). Keep to: four split rows, four caught markers, four missed markers, one descending miss-pathway arrow, one terminal residual box. Use 1pt strokes, single-headed arrows only, white background, no text anywhere. Flat vector, Okabe-Ito palette only.

### Block 2 — Full SCOPE Prompt
[S - Specification] single-column 89mm textbook width, 300 DPI, vector, white background, unannotated.
[C - Content] Four layer rows each split into caught (left) and missed (right); a descending pathway carrying misses down through every row into a terminal residual box of three uncaught markers. Preserve: per-layer caught/missed split and the accumulation of misses into the residual.
[O - Organization] Four stacked rows with a vertical caught|missed divider; one continuous downward arrow on the missed side into the bottom residual box.
[P - Presentation] Flat vector, Okabe-Ito: caught markers Bluish Green #009E73, missed markers Vermillion #D55E00, miss-pathway arrow neutral light gray, residual box and its markers Vermillion #D55E00; 1pt strokes; single-headed arrows only; no text in image.
[E - Exclusions] No layer names, no "catch"/"miss" words, no failure-class names, no legend, no chapter numbers.

### Block 3 — Negative Prompt
layer names, catch/miss words, failure-class names, legend, chapter numbers, text labels, words, gibberish letters, titles, captions, decorative borders, realistic textures, plastic wrap effects, drop shadows, gradient backgrounds, photographic elements, non-standard arrows, dual-headed arrows, hand-drawn styles, sketch lines, human figures, visual clutter, overlapping unaligned paths, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Video Candidate Pass
FIGURE 1 — seven-step decision flow: VIDEO CANDIDATE | STATIC SUFFICIENT — sequential procedure of ≥3 steps — a static top-to-bottom flowchart fully conveys the order; a build-on-reveal animation would aid teaching but is not required.
FIGURE 2 — the box that never closes: STATIC SUFFICIENT — single structural thesis-image — the open-bottomed box is a fixed picture whose whole force is read in one frame.
FIGURE 3 — the strictness dial: STATIC SUFFICIENT — side-by-side comparison — the height difference and added bands are spatial and legible at rest.
FIGURE 4 — per-layer catch/miss accounting: VIDEO CANDIDATE | STATIC SUFFICIENT — a propagation pathway (misses flowing down) — a short animation of misses dropping layer to layer into the residual would dramatize the accumulation, though the static arrow carries it adequately.

Recommendation: Produce all four as static figures; Figures 1, 2, and 3 are Critical (the decision flow, the thesis-image, and the strictness dial are the chapter's load-bearing visuals) and should be drawn first. Figure 2's open-bottomed box is the single most important image in the book's close and must preserve the missing bottom edge.
